import React from 'react'
import usestate from 'rec'

const autocouter = () => {
    const [count, setCount] = useState(0)

  return (
    <div>
      <p></p>
    </div>
  )
}

export default autocouter
